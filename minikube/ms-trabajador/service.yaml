apiVersion: v1
kind: Service
metadata:
  name: ms-trabajador
spec:
  selector:
    app: ms-trabajador
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP 
  # loadbalancer
