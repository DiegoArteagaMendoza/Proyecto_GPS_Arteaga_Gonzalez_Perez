apiVersion: v1
kind: Service
metadata:
  name: ms-venta
  namespace: backend
spec:
  type: NodePort
  selector:
    app: ms-venta
  ports:
    - protocol: TCP
      port: 8005
      targetPort: 8005
      nodePort: 30085

